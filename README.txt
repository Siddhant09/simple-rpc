Run the server and the client on any two machines.

=======================================================

Commands to compile and run the program:

Compile: make -f Makefile.add

Clean: make -f Makefile.add clean

Run Server: ./add_server

Run Client: ./add_client tesla 

Replace tesla with the name of the machine on which the server is running.